import { absoluteUrl, site } from "./site";
import type { Post } from "./types";
export const personSchema = () => ({ "@context": "https://schema.org", "@type": "Person", "@id": absoluteUrl("/about#person"), name: site.name, url: absoluteUrl("/about"), image: absoluteUrl("/images/portrait.webp"), description: site.authorBio, sameAs: [site.linkedIn], knowsAbout: ["Investment", "Behavioural finance", "Economics", "Public policy"] });
export const websiteSchema = () => ({ "@context": "https://schema.org", "@type": "WebSite", "@id": absoluteUrl("/#website"), name: site.name, url: site.url, description: site.description, inLanguage: "en-GB", publisher: { "@id": absoluteUrl("/about#person") } });
export const articleSchema = (post: Post) => ({ "@context": "https://schema.org", "@type": "BlogPosting", headline: post.title, description: post.description, image: absoluteUrl(post.image), datePublished: post.publishedAt, dateModified: post.updatedAt, author: { "@type": "Person", "@id": absoluteUrl("/about#person"), name: post.author, url: absoluteUrl("/about") }, publisher: { "@type": "Person", name: site.name, url: absoluteUrl("/about") }, mainEntityOfPage: absoluteUrl(`/writing/${post.slug}`), inLanguage: "en-GB", articleSection: post.category, keywords: post.tags.join(", "), isAccessibleForFree: true });
export const collectionSchema = (title: string, path: string, items: { title: string; path: string }[]) => ({ "@context": "https://schema.org", "@type": "CollectionPage", name: title, url: absoluteUrl(path), inLanguage: "en-GB", mainEntity: { "@type": "ItemList", itemListElement: items.map((item, index) => ({ "@type": "ListItem", position: index + 1, name: item.title, url: absoluteUrl(item.path) })) } });
